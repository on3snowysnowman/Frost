This file tracks specific details on what was changed each git push.


32:

   - Wrote a DrawHandler that now handles the "drawing" to the screen, which 
     was originally present in the TextureHandler. This should be a bit more
     clear and intuitive. 

   - Removed Frost::Color class, modified engine to simply use SDL_Color instead.

   - Made TextureHandler now in charge of completely creating font and atlas, 
     along with fixing the issue of some sequences of characters rendering into
     special characters in the final glyph, like "-->" turning into an actual 
     arrow sprite. 


31:

   - Added set method for font and point size to the COH. 

   - Added methods for getting and setting the initialization data to the Engine.

   - Fixed InputHandler not updating the bool for checking if available keys were already generated
     per arbitrary frame. 

   - Added class documentation for TextRenderingHandler.

   - Changed 'display_colored_int' to now be templated, along with renaming it to 
     'generate_colored_int'. This method now returns a ColorString, so that the calculation process
     only needs to be done on-request instead of each frame.

30:

   - Reconfigured all UIItems to use pointers to the COH and cursor color. Additionally, removed 
     the deletion from the base constructor.

   - Began work on a SettingsMenuDemo.

   - All crashes now use the CrashOutputHandler now instead of directly outputting the error 
     details using the TextFileHandler.

   - Changed the "Color.hpp" filename to "Fr_Color.hpp" for separation from potential multiple 
     library name conflicts. Additionally, wrapped the Color struct inside the Frost namespace for 
     the same purpose. 

   - Changed the initialization of the SDL Window to open on the primary monitor. 

   - Renamed ExceptionOutputHandler to CrashOutputHandler for more clarity. The CrashOutputHandler 
     doesn't need to only be used for exception handling output.

   - Finished base Tilemap class. 

29:

   - Fixed TilemapDemo using the correct add entity function.

28:

   - Add a _user_update method to the FrostEngine class that users of the Engine can
     override to place components in to update each frame.

   - Changed the minimum font size to 11.

   - Added a method to the COH and TRH for retrieving the current font path.

   - Added an ExceptionOutputHandler class.


27:

   - Reconfigured TextRenderingHandler to now properly create the texture atlas at different
     sizes. 

   - Removed non monospaced fonts from the fonts file.

   - ConsoleOutputHandler now supports changing fonts. 

   - Added new fonts.

   - Added an ability to ConsoleOutputHandler and TextRenderingHandler to retrieve the fonts
     from their internal fonts path.


26:

   - Added a method to ConsoleOutputHandler to change the font size.

   - Rewrote EventHandler to handle multiple subscriptions to one invoke token. Additionally,
     removed the dynamic cast and replaced it with a static cast for increased performance.

   - Rewrote WeightedDistribution, along with making it, Fr_Random and Fr_Math to templated
     methods when possible.

   - Finished setting and modifying methods inside WeightedDistribution.

   - Fixed FrostRandom::generate_random_int seg faulting when the higher bound was less
      than the lower bound.

   - WeightedDistribution now re-sums the weights correctly when they are modified.

25:

   - Began adding a set of methods in the WeightedDistribution class for setting and modifying 
     weights and values.

24:

   - Fixed typos along with redundant Demo method in SpriteHandler class.
   - Added a new method to the FileSystemHandler for getting file sizes.   
   - Added separate shell file to install asset and data directories. This way, cmake no longer 
     installs them as root and unaccessible. 
   - Added another sample method to the WeightedDistribution class for sampling from a specified
     index range.


23:

   - Fixed static fiasco with TextFileHandler by adding Construct on First Use methods.

22:

   - Added a WeightDistribution class for generating random numbers with set probabilities.

21:

   - Added a weighted distribution test file for testing weighted distribution.

20:

   - Reconfigured build files such that the engine has its own build script and can be ran 
     independently of the Demo build scripts.

19:
   
   - Configured the main engine to have its own build file for only building the engine and 
     installing it to disk.

   


18:

   - Finished reconfiguring SpriteHandler.


17:

   - Continued work on redesigning SpriteHandler.

16:

   - Began redesigning SpriteHandler.

15:

   - Removed old content from README.


14:

   - Added some file identifiers to those that didn't have them.

   - Added proper move and copy constructors along with operator= functions to TextRenderingHandler
     and ConsoleOutputHandler.


13:

   - Fixed the SDL_Renderer background not properly being set.

   - Implemented a basic TextRenderingHandler using SDL_ttf. Reconfigured the ConsoleOutputHandler 
     to utilize this new TextRenderingHandler.

   - Added function to the TextureHandler that saves a static SDL_Texture to disk as a png.

12:

   - Began work on a new TextRenderer that uses SDL_ttf. 

   - Made a new Demo file.


11:

   - Fixed naming in TimeObserver.
 
   - Added typedef to TimeObserver for ease of access to chrono time points.
 
   - Added a "Redesigns" folder for classes that are being redesigned/overhauled but can't be 
     integrated into the engine yet, due to naming conflicts.

   - Fixed typo(s) in documentation.


10:

   - Added high precision timing to TimeObserver using chrono.


9: 

   - Fixed bug in ConsoleOutputHandler's move_cursor and move_cursor_y where it was setting the
     first cursor's position to 0, not the second.

   - Added a Component class. 


8:

   - Began work on a Tilemap architecture. 

   - Fixed issue with UIDisplayTools meter methods not properly calculating meter numbers.

   - Removed some unused variables.

   - Removed several redundant allocations of vector and unordered_map on the heap. 

   - Began work on an EventSystem

   - Configured cmake to automatically find and compile src files rather than directly declaring 
     them.


7 :

   - Fixed documentation of TextFileHandler::fetch(), along with a invalid nullptr return in its 
     implementation.

6 :

   - Reconfigured text font files to use ints to resemble characters rather than strings, also 
     reconfigured the way TextRenderingHandler reads them.

   - Added a new font, "Other_Font". Most likely will become the default one, looks much nicer
     than my custom made one. 

   - Changed the default vertical space modifier for the ConsoleOutputHandler to be 1.0f, since
     the new font supplies space vertically between characters.

5 :

   - Fixed typos in README.txt

   - Fixed bug in color constructor when initializing color name. 

   - Added documentation for FrostRandom.

   - Updated documentation for TextFileHandler.hpp

4 :

   - Added a FrostRandom class under Fr_Random for getting random numbers.

   - Added get_anchor() method to ConsoleOutputHandler.

   - Reconfigured the way cmake installs to the disk and handles building.

   - Added UIButton. UISimulation should now be fully functional.


3 :
   
   - Finished UISimulationTools.

   - Created a shell script for automatic building on arch linux.

   - Fixed bug in MenuManager where a wierd issue was happening with a const Menu*.


2 : 

   - Began work on UISimulationTools.

   - Added set_anchor_here to ConsoleOutputHandler.

   - Finished UITextList, and started work on UIList.

   - Added is_even to Fr_Math.

   - Removed redundant block in JsonHandler::dump that created a file if it didn't exist, which is
     done automatically anyways by std::fstream.

   - Removed typo in UIChoice::m_selected_index's description.

1 :

    - Began work on a UITextList class.

    - Added a UIChoice class. 

    - Reconfigured UIVariable into three distinct classes, UIStringVariable UIIntVariable and 
      UIFloatVariable. This will remove all type checking logic that had to exist in the original
      UIVariable, since now there is a class for each specific type.

0 : 
    - Added GitLog.txt.
    - Finished UIVariable. 
    - Added a new method for handling ctrl-backspace behavior in Fr_StringManip.
